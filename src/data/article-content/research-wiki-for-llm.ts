export const researchWikiForLlmContent = `

지난 1년 반 동안 쌓인 유저 리서치 자료를 다시 꺼내볼 일이 있었다. 사용성 테스트, 심층 인터뷰, 컨셉 테스트, 설문, VOC 주간 분석. 폴더를 열어보니 PDF, PPT, 녹취록 텍스트 파일, 마크다운, 컨플루언스 문서가 뒤섞여 있었고 한 건이 수십 페이지짜리인 것도 있었다.

다 읽기에는 너무 많고, 그렇다고 통째로 LLM에 밀어넣기에도 너무 컸다. 그래서 이걸 LLM이 빠르고 쉽게 읽을 수 있는 위키로 정리해보기로 했다.

---

## Open Knowledge Format과 LLM Wiki

팀장 덕분에 LLM에 특화된 문서 포맷 두 가지를 접하게 되었다.

하나는 구글의 **[Open Knowledge Format](https://cloud.google.com/blog/products/data-analytics/how-the-open-knowledge-format-can-improve-data-sharing)**. 지식을 담는 또 하나의 서비스를 만들지 말고 형식을 정하자는 이야기다. 마크다운 파일 하나가 개념 하나고, YAML 프론트매터로 최소한의 메타데이터만 통제하고, 나머지는 쓰는 사람이 알아서 정한다. 강제하는 필드는 문서 타입 하나뿐. 파일 경로가 계층이 되고 마크다운 링크가 그래프가 된다. 벡터 DB도 SDK도 없이 사람과 에이전트가 같은 파일을 읽는다.

다른 하나는 안드레이 카파시의 **[LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)** 노트. 질문할 때마다 원본에서 청크를 긁어오지 말고, LLM이 위키를 계속 고쳐 쓰게 하자는 것. 새 자료가 들어오면 읽고, 기존 페이지를 갱신하고, 상호 참조를 유지하고, 모순이 보이면 표시해둔다.

---

## 문서 구조

계층부터 정했다. 원본 리서치 데이터는 불변 계층으로 두고 절대 손대지 않는다. 그 위에 정리된 위키 계층을 올리고, 맨 위에 구조와 규칙을 정의하는 스키마 문서를 뒀다.

원본 한 건당 요약 페이지 하나인 **소스(source)**, 서비스나 기능처럼 리서치가 다루는 대상인 **엔티티(entity)**, 여러 리서치를 가로지르며 반복되는 패턴인 **개념(concept)**. 페이지끼리는 평범한 마크다운 링크로 연결했다.

모든 페이지는 YAML 프론트매터로 시작한다. 문서 타입은 열네 가지로 통제했고 나머지 필드는 선택이다. 본문 구조도 고정했다. 조사 방법·표본 수·조사 시점을 담은 표, 핵심 발견 목록, 관련 페이지 링크 순. 이렇게 하니 페이지 하나가 그대로 하나의 청크가 됐다.

전체 구조는 대략 이런 모양이다.

~~~
[ 세 개의 계층 ]

   스키마 — 구조·명명·인제스트 절차를 정의한다
      :        (사람과 에이전트가 함께 고쳐나간다)
      :  규칙
      v
   위키 — 사람은 읽고, 에이전트가 쓴다
      ^
      :  ingest — 원본을 읽고 아래 세 축에 나눠 적는다
      :
   원본 — 불변. 절대 수정하지 않는다


[ 위키를 이루는 세 축 ]

   소스 (source) — 원본 1건당 요약 페이지 하나
   엔티티 (entity) — 리서치가 다루는 대상 (서비스·기능·브랜드)
   개념 (concept) — 리서치를 가로지르며 반복되는 패턴
              근거 건수를 함께 적는다
              근거가 1건뿐이면 승격하지 않는다

   +-- 셋은 마크다운 링크로 서로를 참조한다. 그 링크가 곧 그래프다.
       벡터 DB도 임베딩도 없다. 파일 경로가 곧 개념의 정체성이다.


[ 페이지 하나의 형식 ]

   ---
   type       <- 14종으로 통제된 문서 타입 (필수)
   title / description / tags / timestamp / resource
   ---
   ## 리서치 정보 — 조사 방법 · 표본 수 · 조사 시점 · 원본 링크
   ## 핵심 발견 — 번호 매긴 목록
   ## 관련 페이지 — 엔티티 · 개념으로 나가는 링크

   +-- 형식이 일정하니 페이지 하나가 그대로 하나의 청크가 된다
~~~


모든 인용에는 리서치 방법과 표본 수를 붙였고, 개념 페이지에는 그 개념을 뒷받침하는 근거가 몇 건인지 적어뒀다.

VOC 인용문에는 원본 응답 ID를 달아서 어떤 문장이든 원본까지 되짚어갈 수 있게 했다.

그리고 근거가 한 건뿐인 패턴은 개념으로 등록하지 않는다. 한 번 본 걸 개념 페이지로 올려두면 다음에 그걸 읽는 사람도 에이전트도 "우리가 아는 사실"로 받아들이게 된다. 일단 소스 페이지에 남겨두고, 다음 리서치에서 다시 확인되면 그때 독립시킨다.

---

리서치 위키는 새 자료가 생길 때마다 매번 업데이트 해주고 있다. "그거 작년에 했던 것 같은데" 하고 리서치 인사이트 하나 찾으려고 30분 넘게 슬랙, 구글 문서, 컨플루언스 등을 해매는 일은 더 이상 없어졌다.

`
