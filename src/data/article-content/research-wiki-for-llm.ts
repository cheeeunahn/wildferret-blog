export const researchWikiForLlmContent = `## 핵심 요약

- 1년 반 쌓인 PDF·PPT·녹취록 리서치를 원본(불변)·위키·스키마 세 계층으로 나누고, 위키는 소스·엔티티·개념 세 축으로 쪼갰다. 벡터 DB 없이 파일 경로가 계층이고 마크다운 링크가 그래프다.
- 모든 인용에 조사 방법과 표본 수를 붙였고, 근거가 1건뿐인 패턴은 개념으로 승격하지 않았다. 한 번 관찰된 걸 일반화하는 순간이 환각의 씨앗이라서다.
- 이 위키가 나중에 가상 유저 패널의 페르소나 속성을 채우는 재료가 됐다. 열 개가 넘는 칸을 상상으로 메우지 않아도 됐다.

---

사용성 테스트, 심층 인터뷰, 컨셉 테스트, 설문, VOC 주간 분석까지 약 1년 반 정도 걸쳐 많이 쌓여 있는 과거 유저 리서치 자료들. PDF, PPT, 인터뷰 녹취록 형태의 텍스트 파일, 마크다운 파일, 컨플루언스 문서 등 형식도 제각각이고 한 건이 수십 페이지가 되는 경우도 있다. 사람이 다 하나씩 읽기에는 너무 많고, 그대로 LLM 프롬프트에 밀어넣기에도 너무 크다. **그래서 이 쌓여있는 유저 리서치 자료를 의미있게 잘 활용하기 위해 흩어져 있던 리서치를 LLM이 읽을 수 있는 위키로 정리하는 일을 했다.**

---

## 두 편의 글에 기대서 시작했다

위키 형식은 아래 두 가지에 기반했다.

하나는 구글이 제안한 **[Open Knowledge Format](https://cloud.google.com/blog/products/data-analytics/how-the-open-knowledge-format-can-improve-data-sharing)**이다. 요지는 지식을 담는 또 하나의 서비스가 아니라 **형식**이 필요하다는 것. 마크다운 파일 하나가 개념 하나가 되고, YAML 프론트매터로 최소한의 메타데이터만 통제하고, 나머지는 생산자가 알아서 정한다. 강제하는 필드는 문서 타입 하나뿐이다. 파일 경로가 계층이 되고 마크다운 링크가 그래프가 된다. 벡터 DB도 SDK도 없이 사람과 에이전트가 똑같은 파일을 읽는다. 리서치 위키에 필요한 게 정확히 이거였다.

다른 하나는 안드레이 카파시가 쓴 **[LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)** 노트다. 질문할 때마다 원본에서 청크를 긁어오는 대신, LLM이 위키를 계속 고쳐 쓰게 하자는 이야기다. 새 자료가 들어오면 읽고, 기존 페이지를 갱신하고, 상호 참조를 유지하고, 모순을 표시한다.

---

## 계층과 축을 이렇게 나눴다

계층은 다음과 같이 세 가지로 나눴다. (1) 원본은 절대 수정하지 않는 불변 계층으로 두고, (2) 그 위에 정리된 위키 계층을 올리고, (3) 맨 위에 구조와 규칙을 정의하는 스키마 문서를 뒀다.

위키는 리서치별로도 날짜별로도 묶지 않았다. 대신 세 가지 축으로 나눴다. 원본 한 건당 요약 페이지 하나인 **소스(source)**, 서비스나 기능처럼 리서치가 다루는 대상인 **엔티티(entity)**, 여러 리서치를 가로지르며 반복되는 패턴인 **개념(concept)**. 페이지끼리는 평범한 마크다운 링크로 연결했다. 벡터 DB도 임베딩 파이프라인도 없다. 파일 경로가 곧 개념의 정체성이고 링크가 곧 그래프다.

모든 페이지는 YAML 프론트매터로 시작한다. 문서 타입은 열네 가지로 통제했고 나머지 필드는 선택이다. 본문 구조도 고정했다. 조사 방법·표본 수·조사 시점을 담은 표, 핵심 발견 목록, 관련 페이지 링크 순이다. 형식이 일정하니 페이지 하나가 그대로 하나의 청크가 된다.

전체 구조를 그림으로 정리하면 이렇다.

~~~
[ 세 개의 계층 ]

   스키마 — 구조·명명·인제스트 절차를 정의한다
      :        (사람과 에이전트가 함께 고쳐나간다)
      :  규칙
      v
   위키 — 사람은 읽고, 에이전트가 쓴다
      ^
      :  ingest — 원본을 읽고 아래 세 축에 나눠 적는다
      :
   원본 — 불변. 절대 수정하지 않는다


[ 위키를 이루는 세 축 ]

   소스 (source) — 원본 1건당 요약 페이지 하나
   엔티티 (entity) — 리서치가 다루는 대상 (서비스·기능·브랜드)
   개념 (concept) — 리서치를 가로지르며 반복되는 패턴
              근거 건수를 함께 적는다
              근거가 1건뿐이면 승격하지 않는다

   +-- 셋은 마크다운 링크로 서로를 참조한다. 그 링크가 곧 그래프다.
       벡터 DB도 임베딩도 없다. 파일 경로가 곧 개념의 정체성이다.


[ 페이지 하나의 형식 ]

   ---
   type       <- 14종으로 통제된 문서 타입 (필수)
   title / description / tags / timestamp / resource
   ---
   ## 리서치 정보 — 조사 방법 · 표본 수 · 조사 시점 · 원본 링크
   ## 핵심 발견 — 번호 매긴 목록
   ## 관련 페이지 — 엔티티 · 개념으로 나가는 링크

   +-- 형식이 일정하니 페이지 하나가 그대로 하나의 청크가 된다
~~~

---

## 유저 리서치 위키 개념(concept)의 무게 정의하기

다섯 명 인터뷰와 천 명 설문은 무게가 다르다. 그래서 모든 인용에 방법과 표본 수를 붙였고 개념 페이지에는 그 개념을 뒷받침하는 근거가 몇 건인지 적어뒀다. VOC 인용문에는 원본 응답 ID를 달아서 어떤 문장이든 원본까지 되짚어갈 수 있게 했다.

근거가 한 건뿐인 패턴은 개념으로 승격하지 않았다. 한 번 관찰된 걸 일반화하는 순간 그게 환각의 씨앗이 된다. 다음 리서치에서 다시 확인되면 그때 독립시킨다. 오래된 발견이 새 리서치로 뒤집히는 경우도 마찬가지다. 최신을 우선하되 낡은 주장을 지우지는 않고 변경 로그에 남겨둔다.

---

## 단순 위키에서 가상 유저 패널까지

이 위키는 원래 아까워서 시작한 일이었다. 리서치가 쌓이기만 하고 아무도 다시 꺼내보지 않는 게 아깝다는 것 말고 특별한 생각이 본래 없었지만, 비슷한 시기에 동료와 함께 우연히 사내용 가상 유저 패널을 만들고 있었다. 실제 유저 대신 데이터로 만든 합성 페르소나에게 먼저 프로토타입 화면을 보여주거나 신규 서비스에 대한 컨셉 등을 평가받는 도구다. 페르소나마다 열 개가 넘는 속성을 채워야 했는데, 그 칸을 상상으로 메우는 대신 위키에서 꺼내 쓰면 됐다. 어느 세그먼트가 어떤 리서치에서 어떻게 반응했는지가 이미 페이지로 적혀 있으니까. 그리고 근거 1건짜리 패턴을 개념으로 올리지 않는다는 규칙은, 그대로 페르소나가 없는 성향을 지어내지 않게 막는 장치가 됐다.

그 이야기는 [따로 정리해뒀다](/article/synthetic-user-research-panel).
`
