export const researchWikiForLlmContent = `리서치 자료는 이상하게도, 쌓일수록 덜 쓰인다.

사용성 테스트, 심층 인터뷰, 컨셉 테스트, 설문, VOC 주간 분석 — 몇 해에 걸쳐 백 건 가까이 쌓여 있었다. 그런데 정작 새 프로젝트가 시작되면 아무도 그걸 다시 꺼내보지 않는다. 어디에 무엇이 있는지 아는 사람만 알고, 그 사람도 정확히는 기억하지 못한다. 반년 전 인터뷰에서 이미 나온 이야기를, 반년 뒤에 또 물어본다.

리서치 자료는 원래 사람이 읽으라고 쓰인 것들이다. 보고서 PDF, 발표 자료, 인터뷰 녹취록 — 형식도 제각각이고 한 건이 수십 페이지다. 사람이 다 읽기에는 너무 많고, 그대로 LLM 프롬프트에 밀어넣기에도 너무 크다.

그래서 흩어져 있던 리서치를 LLM이 읽을 수 있는 위키로 정리하는 일을 했다.

---

## 두 편의 글에 기대서

형식을 처음부터 발명하지는 않았다. 이미 같은 문제를 정리해둔 글 두 편에 기대서 시작했다.

하나는 구글이 제안한 **[Open Knowledge Format](https://cloud.google.com/blog/products/data-analytics/how-the-open-knowledge-format-can-improve-data-sharing)**이다. 요지는 지식을 담는 또 하나의 서비스가 아니라 **형식**이 필요하다는 것. 마크다운 파일 하나가 개념 하나가 되고, YAML 프론트매터로 최소한의 메타데이터만 통제하고, 나머지는 생산자가 알아서 정한다. 강제하는 필드는 문서 타입 하나뿐이다. 파일 경로가 계층이 되고 마크다운 링크가 그래프가 된다. 벡터 DB도 SDK도 없이 사람과 에이전트가 똑같은 파일을 읽는다. 리서치 위키에 필요한 게 정확히 이거였다.

다른 하나는 안드레이 카파시가 쓴 **[LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)** 노트다. 질문할 때마다 원본에서 청크를 긁어오는 대신, LLM이 위키를 계속 고쳐 쓰게 하자는 이야기다. 새 자료가 들어오면 색인만 하는 게 아니라 읽고, 기존 페이지를 갱신하고, 상호참조를 유지하고, 모순을 표시한다. 역할 분담이 명확하다 — "사람의 일은 출처를 고르고, 분석의 방향을 정하고, 좋은 질문을 던지고, 그게 무슨 의미인지 생각하는 것. 나머지는 전부 LLM의 일이다."

앞의 것에서 문서의 생김새를, 뒤의 것에서 운영 방식을 가져왔다. 그 위에 리서치라는 도메인에 필요한 규칙을 얹었다.

---

## 계층과 축

계층은 셋으로 나눴다. 원본은 절대 수정하지 않는 불변 계층으로 두고, 그 위에 정리된 위키 계층을 올리고, 맨 위에 구조와 규칙을 정의하는 스키마 문서를 뒀다. 원칙은 하나다 — **사람은 읽고, 에이전트가 쓴다.**

위키는 리서치별로도 날짜별로도 묶지 않았다. 대신 세 축으로 쪼갰다. 원본 한 건당 요약 페이지 하나인 **소스**, 서비스나 기능처럼 리서치가 다루는 대상인 **엔티티**, 여러 리서치를 가로지르며 반복되는 패턴인 **개념**. 페이지끼리는 평범한 마크다운 링크로 연결했다. 벡터 DB도 임베딩 파이프라인도 없다. 파일 경로가 곧 개념의 정체성이고 링크가 곧 그래프다.

모든 페이지는 YAML 프론트매터로 시작한다. 문서 타입은 열네 가지로 통제했고 나머지 필드는 선택이다. 본문 구조도 고정했다 — 조사 방법·표본 수·조사 시점을 담은 표, 핵심 발견 목록, 관련 페이지 링크. 형식이 일정하니 페이지 하나가 그대로 하나의 청크가 된다.

전체 구조를 그림으로 정리하면 이렇다.

~~~
[ 세 개의 계층 ]

   스키마 — 구조·명명·인제스트 절차를 정의한다
      :        (사람과 에이전트가 함께 고쳐나간다)
      :  규칙
      v
   위키 — 사람은 읽고, 에이전트가 쓴다
      ^
      :  ingest — 원본을 읽고 아래 세 축에 나눠 적는다
      :
   원본 — 불변. 절대 수정하지 않는다


[ 위키를 이루는 세 축 ]

   소스 — 원본 1건당 요약 페이지 하나
   엔티티 — 리서치가 다루는 대상 (서비스·기능·브랜드)
   개념 — 리서치를 가로지르며 반복되는 패턴
              근거 건수를 함께 적는다
              근거가 1건뿐이면 승격하지 않는다

   +-- 셋은 마크다운 링크로 서로를 참조한다. 그 링크가 곧 그래프다.
       벡터 DB도 임베딩도 없다. 파일 경로가 곧 개념의 정체성이다.


[ 페이지 하나의 형식 ]

   ---
   type       <- 14종으로 통제된 문서 타입 (필수)
   title / description / tags / timestamp / resource
   ---
   ## 리서치 정보 — 조사 방법 · 표본 수 · 조사 시점 · 원본 링크
   ## 핵심 발견 — 번호 매긴 목록
   ## 관련 페이지 — 엔티티 · 개념으로 나가는 링크

   +-- 형식이 일정하니 페이지 하나가 그대로 하나의 청크가 된다
~~~

---

## 가장 신경 쓴 규칙 — 근거 강도

다섯 명 인터뷰와 천 명 설문은 무게가 다르다. 그래서 모든 인용에 방법과 표본 수를 붙였고 개념 페이지에는 그 개념을 뒷받침하는 근거가 몇 건인지 적어뒀다. VOC 인용문에는 원본 응답 ID를 달아서 어떤 문장이든 원본까지 되짚어갈 수 있게 했다.

반대로 근거가 한 건뿐인 패턴은 개념으로 승격하지 않았다. 한 번 관찰된 걸 일반화하는 순간 그게 환각의 씨앗이 된다. 다음 리서치에서 다시 확인되면 그때 독립시킨다. 오래된 발견이 새 리서치로 뒤집히는 경우도 마찬가지다. 최신을 우선하되 낡은 주장을 지우지는 않고 변경 로그에 남겨둔다.

---

## 예상하지 못한 쓸모

이 위키는 원래 아까워서 시작한 일이었다. 리서치가 쌓이기만 하고 아무도 다시 꺼내보지 않는 게 아깝다는 것 말고 다른 목적은 없었다.

그런데 정리해두고 나니 전혀 다른 곳에서 쓰였다. 비슷한 시기에 동료와 함께 **가상 유저 패널** — 실제 유저 대신 데이터로 만든 합성 페르소나에게 먼저 화면을 보여주는 도구 — 을 만들고 있었는데, 그 페르소나에 근거를 붙이는 작업이 훨씬 수월해졌다. "20대 여성이라면 이럴 것 같다"가 아니라 "이 세그먼트는 이 리서치에서 이렇게 반응했다"를 근거로 쓸 수 있게 됐다.

근거 강도 규칙이 여기서 한 번 더 값을 했다. 근거 1건짜리 패턴을 개념으로 올리지 않는다는 규칙은, 그대로 페르소나가 없는 성향을 지어내지 않게 막는 장치가 됐다.

그 이야기는 [따로 정리해뒀다](/article/synthetic-user-research-panel).
`
