export const npsDashboardPipelineContent = `종소세 시즌이 시작되면 NPS 설문 데이터가 쏟아진다. 신청완료, 신고완료, 미대상자 — 세 가지 설문이 동시에 돌아간다. 매일 아침 "어제 NPS 몇이었어?"라는 질문이 나오고, 주관식 응답을 훑어보면서 사용자 반응을 체크해야 한다.

원래는 사내 데이터 웨어하우스에서 SQL을 돌려서 직접 확인했다. 그런데 이걸 볼 사람이 나 혼자가 아니다. 팀원들도 봐야 하고, 경영진도 봐야 한다. 매번 "NPS 몇이야?"라고 물어보는 대신, 누구나 링크 하나로 확인할 수 있는 대시보드가 필요했다.

---

## 출발점 — 로컬에서 돌아가는 대시보드

처음에는 로컬에서 돌아가는 대시보드를 만들었다. Express 서버가 Google Sheets에서 데이터를 읽어오고, React 프론트엔드가 차트를 그리는 구조. NPS 점수 추이, 응답량, 추천자/비추천자 비율, 주관식 피드백까지 한 화면에 담았다.

로컬에서는 잘 돌아갔다. 문제는 배포였다.

우리 팀의 프론트엔드 프로젝트는 모노레포로 관리된다. GitHub Actions로 정적 호스팅에 배포하는 파이프라인이 이미 잡혀 있어서, 여기에 올리면 사내 도메인으로 바로 접근할 수 있다.

그런데 NPS 대시보드에는 Express 백엔드가 있었다. S3는 정적 파일만 서빙할 수 있으니까, 라이브 서버를 띄울 수가 없다.

---

## 해결 — 데이터를 빌드 타임에 굽기

Claude와 논의한 결론은 단순했다. **서버를 없애자.**

Google Sheets에서 데이터를 미리 가져와서 \`data.json\`으로 저장하고, 프론트엔드는 이 정적 파일만 읽으면 된다. 서버가 필요 없으니 S3에 바로 올릴 수 있다.

\`scripts/fetch-data.js\`를 만들었다. Google Sheets API로 11만 행을 읽어서, 일간/주간/월간으로 NPS를 집계하고, 주관식 응답을 정리해서 하나의 JSON 파일로 만든다. 0.32MB. 실행 시간은 몇 초.

프론트엔드의 변경도 간단했다. \`fetch('/api/nps/trend')\`를 \`fetch('data.json')\`으로 바꾸는 게 전부였다.

---

## 배포 — 모노레포에 끼워넣기

모노레포에는 이미 여러 서비스가 들어있다. 새 서비스를 추가하는 건 몇 가지 규칙만 맞추면 된다.

1. 서비스 디렉토리에 코드를 넣는다
2. 빌드 스크립트를 추가한다
3. 배포 워크플로에 서비스 이름을 등록한다

다만 CI 환경의 Node.js 버전이 낮아서, 최신 빌드 도구가 돌아가지 않았다. 호환되는 버전으로 다운그레이드해서 맞췄다. 이런 삽질은 실제로 배포를 돌려봐야 알 수 있다. Claude가 에러 로그를 보고 바로 원인을 짚어줘서, 세 번의 빌드 실패를 30분 안에 해결했다.

---

## 자동화 — 매일 새벽에 데이터가 알아서 바뀐다

정적 대시보드의 단점은 데이터가 빌드 시점에 고정된다는 것이다. 매일 수동으로 데이터를 갱신하고 배포하는 건 말이 안 된다.

GitHub Actions에 크론 워크플로를 하나 더 만들었다. \`refresh-nps-data.yml\`. 매일 새벽 00:03 KST에 자동으로 실행된다.

파이프라인은 이렇다:

1. GitHub Actions가 Google Sheets에서 데이터를 가져온다 (서비스 계정 인증)
2. \`data.json\`이 바뀌었으면 자동으로 커밋하고 push한다
3. push가 성공하면 바로 DEV 배포 워크플로를 트리거한다
4. S3에 새 빌드가 올라가고, CloudFront 캐시가 무효화된다

아침에 출근해서 대시보드 URL을 열면, 어젯밤까지의 NPS 데이터가 이미 반영되어 있다.

여기서 중요했던 건 **인증**이었다. 로컬에서는 내 개인 Google OAuth 토큰을 쓰면 되지만, CI 환경에서는 이게 없다. Google Cloud 서비스 계정의 JSON 키를 GitHub Secrets에 저장하고, \`fetch-data.js\`가 환경에 따라 자동으로 인증 방식을 선택하도록 만들었다. CI에서는 서비스 계정, 로컬에서는 OAuth.

---

## 완성된 대시보드

대시보드에는 네 가지 섹션이 있다.

**NPS 스코어 카드** — 세 설문의 최신 주간 NPS, 응답 수, 추천자/중립자/비추천자 수, 전주 대비 변화. 한눈에 "지금 어떤 상태인지"를 본다.

**NPS 점수 추이** — 일간/주간/월간 토글로 트렌드를 본다. 7일부터 전체까지 범위 조절 가능. 세 설문이 하나의 차트 위에 겹쳐져서 비교가 된다.

**추천자/비추천자 비율** — 설문별 또는 전체로 비율 변화를 추적한다. "NPS가 떨어졌을 때, 추천자가 줄어든 건지 비추천자가 늘어난 건지"를 구분할 수 있다.

**주관식 피드백** — 설문별/주차별 필터링. 각 응답에 NPS 점수가 태그되어 있어서, "비추천자가 뭐라고 했는지"를 바로 볼 수 있다.

---

## PM이 파이프라인을 만든다는 것

이번에 만든 건 대시보드가 아니라 **파이프라인**이다. 데이터 소스(Google Sheets)에서 가공(fetch-data.js)을 거쳐 배포(S3)까지, 사람이 손대지 않아도 매일 돌아가는 자동화.

PM이 이런 인프라를 직접 만들 수 있다는 게 중요하다. 예전이라면 "대시보드 하나 만들어주세요"라고 데이터팀에 요청했을 거다. 백로그에 들어가고, 우선순위가 밀리고, 한 달 뒤에 "아 그거 아직이요"라는 답을 듣는다. 그 사이에 종소세 시즌은 끝난다.

지금은 다르다. 필요한 걸 정의하고, Claude와 대화하면서 직접 만들고, CI/CD까지 붙여서 배포한다. 하루. 데이터팀에 부담을 주지도 않고, 내가 원하는 형태 그대로 만들 수 있다.

물론 한계는 있다. Google Sheets를 데이터 소스로 쓰는 건 임시 방편이고, 데이터가 더 커지면 다른 구조가 필요할 것이다. 하지만 **지금 당장 필요한 것을 지금 당장 만들 수 있다**는 게 핵심이다. 완벽한 솔루션을 기다리는 동안 놓치는 인사이트가 더 비싸다.

---

## 파이프라인 구성도

전체 파이프라인을 정리하면 이렇다.

### 데이터 소스

- Google Sheets 스프레드시트 (약 11만 행의 설문 응답 데이터)
- 컬럼: 날짜, 설문코드, 응답ID, 유저ID, 응답일시, 질문유형, 답변텍스트, 답변점수 등

### 추적 설문

| 설문코드 | 설명 |
|---------|------|
| 종소세 신청완료 설문 | 신청 직후 만족도 |
| 종소세 신고완료 설문 | 신고 완료 후 만족도 |
| 종소세 미대상자 설문 | 서비스 전반 만족도 |

### 데이터 갱신 (CI 크론)

| 단계 | 내용 |
|------|------|
| 트리거 | 매일 새벽 자동 실행 + 수동 트리거 가능 |
| 인증 | Google Cloud 서비스 계정 (GitHub Secrets) |
| 가공 | 스프레드시트 전체 읽기 → 일간/주간/월간 NPS 집계 |
| 계산 | 추천자/중립자/비추천자 수, NPS 점수, 비율(%) |
| 주관식 | 텍스트 응답 추출 + NPS 점수 매칭 |
| 출력 | 정적 JSON 파일 (약 0.3MB) |
| 커밋 | 데이터 변경 시 자동 커밋 → push |
| 배포 | push 성공 시 빌드/배포 워크플로 자동 트리거 |

### 배포 (CI 워크플로)

| 단계 | 내용 |
|------|------|
| 트리거 | 데이터 갱신에서 자동 체이닝, 또는 수동 |
| 빌드 | 모노레포 의존성 설치 → TypeScript 체크 → 프로덕션 빌드 |
| 배포 | 정적 호스팅으로 빌드 결과물 업로드 |
| 캐시 | CDN 캐시 무효화 |

### 프론트엔드 (정적 SPA)

| 항목 | 내용 |
|------|------|
| 데이터 로딩 | 빌드 타임에 생성된 JSON 파일 1회 fetch |
| 백엔드 | 없음 (완전 정적) |
| NPS 스코어 카드 | 최신 주간 NPS, 응답 수, 추천자/중립자/비추천자, 전주 대비 변화 |
| NPS 점수 추이 | 일간/주간/월간 토글, 기간 범위 조절 가능 |
| 비율 추적 | 추천자/비추천자 비율 변화 (설문별/전체) |
| 주관식 피드백 | 설문별/주차별 필터, NPS 점수 태그 표시 |

---

## 배운 것들

돌아보면 몇 가지 교훈이 있다.

**"서버가 필요하다"는 착각이었다.** 처음부터 Express를 넣은 건 습관이었다. API가 있어야 데이터를 가져올 수 있다고 생각했는데, 실제로는 빌드 타임에 한 번만 가져오면 충분했다. 정적 파일로 충분한 문제에 서버를 올리면 복잡도만 늘어난다. "이거 꼭 런타임에 해야 해?"라는 질문을 먼저 던졌으면 삽질을 줄였을 것이다.

**배포 환경은 로컬과 다르다. 항상.** 로컬에서 잘 돌아가는 코드가 CI에서 터지는 건 예상했어야 한다. Node.js 버전, 인증 방식, 환경 변수 — 다 다르다. "내 컴퓨터에서는 되는데?"는 대시보드에서도 예외가 아니었다. CI에서 처음 빌드를 돌리기 전에 환경 차이를 먼저 체크리스트로 만들어뒀으면 빌드 실패 3번이 1번으로 줄었을 것이다.

**인증은 이중으로 짜야 한다.** 로컬에서는 OAuth, CI에서는 서비스 계정. 이걸 처음부터 분기해놓지 않으면 나중에 "왜 CI에서만 안 돼?"라는 상황을 만나게 된다. 데이터 파이프라인에서 인증은 언제나 가장 먼저 설계해야 할 부분이다.

**"일단 돌아가게"가 "영원히"가 된다.** Google Sheets를 데이터 소스로 쓴 건 임시 방편이었다. 근데 잘 돌아가니까 바꿀 이유가 없다. 11만 행도 몇 초면 처리된다. "나중에 바꿔야지"라고 생각했던 것들이 실제로는 충분한 경우가 많다. 과도한 설계보다 지금 작동하는 것에 집중하는 게 맞았다.

---

## 기술 스택 요약

혹시 비슷한 걸 만들고 싶은 분들을 위해 정리해둔다.

- **프론트엔드:** React + TypeScript + 차트 라이브러리
- **데이터:** Google Sheets API → 정적 JSON (빌드 타임 생성)
- **인증:** Google Cloud 서비스 계정 (CI) / OAuth (로컬)
- **배포:** 정적 호스팅 (사내 모노레포 CI/CD)
- **자동화:** GitHub Actions 크론 (매일 자동 실행)
- **파이프라인:** 데이터 갱신 → 자동 커밋 → 자동 배포 (완전 무인)`
